namespace AncientFactory.Core.Types
{
    public enum ResourceQuality
    {
        Impure,   // 1 item per tick (low quality)
        Normal,   // 2 items per tick (medium quality)
        Pure      // 3 items per tick (high quality)
    }
}
